PRAGMA user_version = 0;
PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE "order" (
            "index" INTEGER PRIMARY KEY,
            "values" TEXT,
            "check" TEXT
        );
INSERT INTO "order" ("index", "values", "check") VALUES (1, 'mixed data', 'ok');
COMMIT;
